# TaskHorizon Backend APIs

## Offline mode (RFC)

-   SWR do not fetch
-   The design of APIs do not block the UI (stale-while-revalidate)
-   The APIs in offline mode will not initiate any network request
