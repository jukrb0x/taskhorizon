import { CliDefaultOptions, CommandProvider } from "@tsed/cli-core";
import { CliPrisma } from "../services/CliPrisma";
export interface PrismaContext extends CliDefaultOptions {
    command: string;
}
export declare class PrismaCmd implements CommandProvider {
    cli: CliPrisma;
    $exec(ctx: PrismaContext): Promise<{
        title: string;
        task: () => import("rxjs").Observable<any>;
    }[]>;
}
