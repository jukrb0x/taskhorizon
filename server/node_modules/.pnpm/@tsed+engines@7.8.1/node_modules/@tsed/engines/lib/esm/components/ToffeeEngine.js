import { __decorate } from "tslib";
import { promisify } from "util";
import { ViewEngine } from "../decorators/viewEngine.js";
import { Engine } from "./Engine.js";
let ToffeeEngine = class ToffeeEngine extends Engine {
    $compile(template, options) {
        const compile = promisify(this.engine.str_render.bind(this.engine));
        return (options) => compile(template, options);
    }
    async $compileFile(file, options) {
        const compile = promisify(this.engine.__consolidate_engine_render.bind(this.engine));
        return (options) => compile(file, options);
    }
};
ToffeeEngine = __decorate([
    ViewEngine("toffee")
], ToffeeEngine);
export { ToffeeEngine };
//# sourceMappingURL=ToffeeEngine.js.map