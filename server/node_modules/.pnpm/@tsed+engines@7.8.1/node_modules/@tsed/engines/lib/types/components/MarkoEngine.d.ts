import { Engine, EngineOptions } from "./Engine";
export declare class MarkoEngine extends Engine {
    $cacheOptions(template: string, options: any, fromFile: boolean): any;
    $compileFile(file: string, options: EngineOptions): Promise<any>;
    protected $compile(template: string, options: EngineOptions): any;
}
