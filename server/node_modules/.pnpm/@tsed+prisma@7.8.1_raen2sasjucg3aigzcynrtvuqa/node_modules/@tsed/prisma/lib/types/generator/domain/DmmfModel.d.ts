import { DMMF } from "@prisma/generator-helper";
import { ImportDeclarationStructure } from "ts-morph";
import { DmmfField } from "./DmmfField";
export declare class DmmfModel {
    #private;
    readonly isInputType: boolean;
    readonly model: DMMF.Model;
    readonly modelType: DMMF.InputType | DMMF.OutputType;
    constructor({ isInputType, model, modelType }: any);
    get name(): string;
    get fields(): DmmfField[];
    get importDeclarations(): ImportDeclarationStructure[];
    static getModels(dmmf: DMMF.Document, modelsMap: Map<string, DMMF.Model>, typesMap?: Map<string, DMMF.Model>): DmmfModel[];
    static symbolName(name: string): string;
    addImportDeclaration(moduleSpecifier: string, name: string, isDefault?: boolean): this;
    toString(): string;
}
