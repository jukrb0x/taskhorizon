import { StructureKind } from "ts-morph";
export function transformEnumsToEnums(enumModel) {
    const members = enumModel.values.map((value) => {
        return {
            name: value,
            value: value
        };
    });
    return {
        kind: StructureKind.Enum,
        name: enumModel.toString(),
        trailingTrivia: "\n",
        leadingTrivia: "\n",
        isExported: true,
        members
    };
}
//# sourceMappingURL=transformEnumsToEnums.js.map