export declare type Result = [file: string, directory: string];
/** Fetch the file and directory paths from a path, uri, or `import.meta.url` */
export declare function filedirnameFromPath(path: string): Result;
/** Fetch the file and directory paths from an Error instance. */
export declare function filedirnameFromError(error: Error): Result;
/** Fetch the file and directory paths from the caller. */
export declare function filedirnameFromCaller(): Result;
/** Fetch the file and directory paths from the caller. */
declare function filedirname(): Result;
/** Fetch the file and directory paths from the Error instance. */
declare function filedirname(arg: Error): Result;
/** Fetch the file and directory paths from a path, uri, or `import.meta.url`. */
declare function filedirname(arg: string): Result;
export default filedirname;
//# sourceMappingURL=index.d.ts.map