import { CallbackWrapper } from "./Perf";
export declare type LEVELS = Record<number, "black" | "red" | "green" | "yellow" | "blue" | "magenta" | "cyan" | "white" | "gray">;
export declare class PerfLogger {
    #private;
    readonly label: string;
    readonly levels: LEVELS;
    constructor(label?: string, levels?: LEVELS);
    static get(label: string): any;
    start(): this;
    log(...args: any[]): this;
    bind(instance: any): any;
    wrap<T = any>(fn: CallbackWrapper<T>, name?: string): T;
    end(): this;
    private formatLog;
    private formatDiff;
}
