/**
 * @ignore
 * @param viewPath
 * @param conf
 */
export function indexMiddleware(viewPath, conf) {
    return async (ctx) => {
        const { path = "/", options = {}, showExplorer, cssPath, jsPath, urls } = conf;
        ctx.response.body(await ctx.response.render(viewPath, {
            spec: {},
            url: `${path}/swagger.json`,
            urls,
            showExplorer,
            cssPath,
            jsPath,
            swaggerOptions: options
        }));
    };
}
//# sourceMappingURL=indexMiddleware.js.map