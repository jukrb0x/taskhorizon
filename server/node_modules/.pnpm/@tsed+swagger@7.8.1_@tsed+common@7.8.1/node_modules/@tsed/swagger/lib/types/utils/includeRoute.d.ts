import { Provider } from "@tsed/di";
import { SwaggerSettings } from "../interfaces/SwaggerSettings";
export declare function includeRoute(route: string, provider: Provider, conf: SwaggerSettings): boolean;
