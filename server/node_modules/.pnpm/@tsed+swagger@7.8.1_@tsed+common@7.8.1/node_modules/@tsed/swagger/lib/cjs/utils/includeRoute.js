"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.includeRoute = void 0;
const matchPath_1 = require("./matchPath");
function includeRoute(route, provider, conf) {
    const hidden = provider.store.get("hidden");
    const docs = provider.store.get("docs") || [];
    const { doc, pathPatterns } = conf;
    const inDoc = doc && docs.indexOf(doc) > -1;
    const pathPatternMatch = pathPatterns && (0, matchPath_1.matchPath)(route, pathPatterns);
    if (hidden) {
        return false;
    }
    if (!doc && !pathPatterns) {
        return true;
    }
    return !!(inDoc || pathPatternMatch);
}
exports.includeRoute = includeRoute;
//# sourceMappingURL=includeRoute.js.map