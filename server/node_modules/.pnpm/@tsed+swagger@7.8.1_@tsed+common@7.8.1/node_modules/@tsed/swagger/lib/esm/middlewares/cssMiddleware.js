import Fs from "fs";
import { resolve } from "path";
/**
 * @ignore
 * @param path
 */
export function cssMiddleware(path) {
    return (ctx) => {
        const content = Fs.readFileSync(resolve(path), { encoding: "utf8" });
        ctx.response
            .setHeaders({
            "Content-Type": "text/css"
        })
            .status(200)
            .body(content);
    };
}
//# sourceMappingURL=cssMiddleware.js.map