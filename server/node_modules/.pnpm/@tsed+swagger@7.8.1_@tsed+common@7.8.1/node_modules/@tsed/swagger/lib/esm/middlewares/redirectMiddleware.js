/**
 * @ignore
 * @param path
 */
export function redirectMiddleware(path) {
    return (ctx) => {
        if (ctx.request.url === path && !ctx.request.url.match(/\/$/)) {
            ctx.response.redirect(302, `${path}/`);
        }
    };
}
//# sourceMappingURL=redirectMiddleware.js.map