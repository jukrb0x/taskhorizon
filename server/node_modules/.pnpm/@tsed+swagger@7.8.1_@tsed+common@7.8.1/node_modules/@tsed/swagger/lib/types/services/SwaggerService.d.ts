import { Configuration, InjectorService, Platform } from "@tsed/common";
import { OpenSpec2, OpenSpec3 } from "@tsed/openspec";
import { SwaggerOS2Settings, SwaggerOS3Settings, SwaggerSettings } from "../interfaces/SwaggerSettings";
export declare class SwaggerService {
    #private;
    private injectorService;
    private platform;
    private configuration;
    constructor(injectorService: InjectorService, platform: Platform, configuration: Configuration);
    /**
     * Generate Spec for the given configuration
     * @returns {Spec}
     */
    getOpenAPISpec(conf: SwaggerOS3Settings): Promise<OpenSpec3>;
    getOpenAPISpec(conf: SwaggerOS2Settings): Promise<OpenSpec2>;
    getOpenAPISpec(conf: SwaggerSettings): Promise<OpenSpec2>;
}
