import { OnRoutesInit, Configuration, InjectorService, OnReady, PlatformApplication } from "@tsed/common";
import { Env } from "@tsed/core";
import { SwaggerSettings } from "./interfaces/SwaggerSettings";
import { SwaggerService } from "./services/SwaggerService";
/**
 * @ignore
 */
export declare class SwaggerModule implements OnRoutesInit, OnReady {
    injector: InjectorService;
    app: PlatformApplication;
    configuration: Configuration;
    swaggerService: SwaggerService;
    env: Env;
    private loaded;
    get settings(): SwaggerSettings[];
    /**
     *
     */
    $onRoutesInit(): void;
    $onReady(): void;
    generateSpecFiles(): Promise<void[]>;
    private getUrls;
    /**
     *
     * @param conf
     * @param urls
     */
    private createRouter;
    private middlewareSwaggerJson;
}
