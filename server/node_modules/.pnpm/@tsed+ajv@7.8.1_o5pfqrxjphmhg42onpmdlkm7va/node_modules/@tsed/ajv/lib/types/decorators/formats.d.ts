import { AsyncFormatDefinition, FormatDefinition } from "ajv";
export declare type FormatsOptions = Omit<FormatDefinition<any>, "validate" | "compare"> | Omit<AsyncFormatDefinition<any>, "validate" | "compare">;
/**
 * Create a new custom formats validator
 * @param name
 * @param options
 * @decorator
 * @ajv
 */
export declare function Formats(name: string, options?: FormatsOptions): ClassDecorator;
