import { __decorate } from "tslib";
import { objectKeys } from "@tsed/core";
import { JsonMapper } from "../decorators/jsonMapper.js";
/**
 * Mapper for the `Set` Type.
 * @jsonmapper
 * @component
 */
let SetMapper = class SetMapper {
    deserialize(data, ctx) {
        const obj = new Set();
        objectKeys(data).forEach((key) => {
            obj.add(ctx.next(data[key]));
        });
        return obj;
    }
    serialize(data, ctx) {
        const array = [];
        data.forEach((value) => array.push(ctx.next(value)));
        return array;
    }
};
SetMapper = __decorate([
    JsonMapper(Set)
], SetMapper);
export { SetMapper };
//# sourceMappingURL=SetMapper.js.map