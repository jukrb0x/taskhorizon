import { __decorate } from "tslib";
import { JsonMapper } from "../decorators/jsonMapper.js";
/**
 * Mapper for `Array` type.
 * @jsonmapper
 * @component
 */
let ArrayMapper = class ArrayMapper {
    deserialize(data, options) {
        return [].concat(data).map((item) => options.next(item));
    }
    serialize(data, options) {
        return [].concat(data).map((item) => options.next(item));
    }
};
ArrayMapper = __decorate([
    JsonMapper(Array)
], ArrayMapper);
export { ArrayMapper };
//# sourceMappingURL=ArrayMapper.js.map